{
  "name": "server",
  "version": "1.0.0",
  "description": "backend for employee details site",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 4000"
  },
  "keywords": [
    "node",
    "js",
    "express"
  ],
  "author": "Hari",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
